# 项目讲稿：LLM-DeepFM-CTR

---

## 一、开场（1 分钟）

> 大家好，今天我讲的项目是《基于 LLM 语义向量的双通道 DeepFM 冷启动 CTR 预测》。
>
> 核心就一件事：**把 LLM 对电影的理解，灌进 CTR 模型里，解决冷启动问题。**
>
> 做推荐系统的都知道，新用户和新物品没有交互历史，ID 特征学不到东西，AUC 会断崖式下跌。传统方案要么上 UGC 内容画像，要么用 Graph Embedding，但这里我们试了一条新路——直接用 LLM 的语义向量。

---

## 二、第1层 · 功能层：跑通了吗？（1 分钟）

> 整个流程分成 4 步：
>
> **第一步，数据预处理。** 读 MovieLens-1M，80 万评分数据，按时间排序后 8:2 切分。这里特别注意——不是随机切分，推荐系统是时序问题，用未来预测过去就是数据泄漏。
>
> **第二步，LLM 向量生成。** 把 3706 部电影标题+类型传给智谱的 Embedding-3 API，拿到 1024 维向量，存成 npy 矩阵。
>
> **第三步，三组模型训练。** 一个 Baseline（只用 ID 特征），一个双通道（ID + 1024 维 LLM），一个 PCA 消融（ID + PCA 压缩到 64 维）。三个模型的超参数完全一致，唯一变量就是"有没有 LLM 向量"和"要不要 PCA 压缩"。
>
> **第四步，评估可视化。** 算全局 AUC 和分层 AUC，出三张对比图。
>
> **结果怎么样？**
> - Very Cold 层：Baseline 0.5680 → 双通道 **0.6093**，提升 4.13%
> - Cold 层：0.6558 → **0.6692**，提升 1.34%
> - Warm 层：基本持平
> - PCA 消融版：增益折半以上
>
> 结论很清楚：LLM 语义向量有效，而且**完整 1024 维 > PCA 压缩**。

---

## 三、第2层 · 系统层：怎么设计的？（2 分钟）

> 系统层拆成 4 个模块，数据单向流动：

### 模块 A：数据预处理

> 三个关键设计：
>
> **1. 按时间序切分。** 很多人做推荐系统用随机切分，这是错的。评分数据天然有时序，训练集必须是过去，测试集必须是未来。我们的做法是 sort by timestamp，前 80% 训练，后 20% 测试。
>
> **2. LabelEncoder 编码。** DeepCTR 的 SparseFeat 要求类别特征是 0 到 N-1 的连续整数，所以对所有类别特征做编码。注意要存下编码器，线上新用户进来要用同一个编码器映射。
>
> **3. 冷启动分层。** 不是什么新概念，但定义要讲清楚。我们按电影交互次数分了 3 档：
> - Very Cold < 5 次
> - Cold 5~20 次
> - Warm ≥ 20 次
>
> 这个阈值是经验值，工业界一般用 5 次以内算极冷，10~30 次算冷。面试可能会问你为什么是 5 和 20——5 次足够 FM 学到一点信号，20 次以上基本稳定。

### 模块 B：LLM 语义向量

> **为什么选 LLM 不是 Word2Vec？** Word2Vec 只能学词共现，"Toy Story"和"Animation"在 Word2Vec 里的关系就是"两个词经常一起出现"。LLM 能理解"Toy Story 是皮克斯的动画电影，适合儿童观看"这种语义。
>
> **为什么 1024 维？** 智谱 API 支持 256/512/1024。越大信息越完整，而且 DNN 第一层本身就是个 projection，模型会自己学哪些维度对 CTR 有用。
>
> **为什么批量调用？** 3706 部电影单条调 API 要 3706 次 HTTP，批量 64 条只要 61 次，快 10 倍以上。
>
> **为什么存成矩阵不是字典？** 推理时 matrix[movie_id] 是 O(1) 索引，字典有 hash 开销，虽然差别不大但矩阵更规整。

### 模块 C：三组模型训练

> 这里其实是消融实验的标准做法：**保持其他条件不变，只改一个变量。**
>
> Baseline → 双通道：加 LLM 向量，看涨了多少
> 双通道 → PCA：降维方式不同，看完整信息是否必要
>
> 三个模型的结构完全一样：DeepFM，dnn_hidden_units=(256,128)，dnn_dropout=0.3，batch_size=4096，epochs=5。唯一的区别是 DenseFeat 的 dimension。
>
> **为什么选 DeepFM？** Wide & Deep 需要手动构造交叉特征，DeepFM 的 FM 部分自动学二阶交叉。而且代码成熟，DeepCTR-Torch 直接调用。
>
> **为什么 LLM 向量不进 FM？** FM 做两两内积，1024 维 dense 特征进 FM 会产生 52 万个交叉项，计算量和噪声都太大。所以 FM 部分只用 sparse，DNN 部分同时接收 sparse embedding + dense LLM 向量——这就是"双通道"的含义。

### 模块 D：评估

> 评估分两层：全局 AUC 和分层 AUC。全局 AUC 是整体指标，但冷启动的 AUC 差异会被热启动样本稀释，所以必须看分层。
>
> 这里有个容易被问的问题：**为什么整体 AUC 只有 0.7445，但 Very Cold 层 0.6093 反而低很多？** 说明模型对低频物品确实不行，这正是我们想解决的问题。

---

## 四、第3层 · 工程层：上线怎么办？（1.5 分钟）

> 这个项目是实验验证性质，但面试官一定会问：**你这个方案能上线吗？**

### 性能

> 80 万样本 5 个 epoch，几分钟就跑完，embedding 矩阵只有 15MB，这个量级完全没问题。
>
> 但如果数据量扩大 10 倍——用户 6 万→60 万，电影 3706→3.7 万，训练数据 800 万→8000 万，问题就来了：
> - pandas 处理 8000 万行会 OOM，要换 Spark
> - embedding 矩阵从 15MB 变 150MB，还行
> - Sparse embedding 表会膨胀，6 个特征 vocabulary 各涨 10 倍，GPU 显存可能吃紧
> - DeepFM 本身参数主要在 DNN 部分，跟数据量无关，所以模型大小基本不变

### 成本

> LLM API 调用不到 1 块钱——3706 部电影，智谱 Embedding-3 很便宜。而且这是**一次性成本**，电影库不会每天新增几千部。
>
> 3.7 万部电影也才 10 块钱左右，成本根本不是瓶颈。

### 稳定性

> 这是**最需要坦诚的地方**。当前项目有 3 个没做好的地方：
>
> **1. 没固定随机种子。** DeepFM 的 embedding 初始化是随机的，不固定种子每次结果不一样，没法做严格的 A/B 复现。
>
> **2. 没持久化 LabelEncoder。** 代码里 fit 完没保存，线上新用户进来没法做同样的编码。正确的做法是用 joblib.dump 存 pkl 文件。
>
> **3. 没处理 unseen 值。** 测试集可能出现训练集没见过的 movie_id，当前代码直接查 embedding 矩阵会越界。工业界做法是预留一个 `<UNK>` embedding，所有冷启动物品共享。
>
> 如果面试问"还有什么可以改进"，这三个点可以直接说。

### 可观测 & 部署

> 当前只打印了最终 AUC，没有记录训练过程的 loss 曲线。上线的话需要用 MLflow 或 TensorBoard 记录每轮的 AUC。
>
> 部署方案：DeepFM 导出 ONNX → TensorRT 加速 → Redis 缓存 embedding 矩阵。推理延迟可以控制在 10ms 以内。

---

## 五、横向对比：为什么选这个方法？（1.5 分钟）

> **Q：为什么用 LLM 向量，不用协同过滤？**
>
> 协同过滤的问题：冷启动物品压根没有交互历史，UserCF 找不到相似用户，ItemCF 找不到相似物品。LLM 向量的优势是**不需要交互**，光看标题和类型就能算语义相似度。
>
> **Q：为什么不用 Graph Embedding（Node2Vec / PinSage）？**
>
> Graph Embedding 也需要交互图，冷启动节点在图中是孤立点，学不到好的表示。LLM 向量是 side information，跟图结构无关。
>
> **Q：为什么是 DeepFM 不是 Transformer？**
>
> CTR 场景下，DeepFM 在 80 万数据量上已经够用，参数量适中（约 300K），几分钟训完。Transformer 需要更多数据和算力才能发挥优势，小数据量上容易过拟合。
>
> **Q：PCA 为什么不 work？**
>
> PCA 是按方差最大的方向压缩，但**方差大不等于对 CTR 有用**。比如电影的"年份"信息方差很大，但对用户点击行为几乎没影响。而某些细微的语义差异——比如"温馨家庭片"vs"黑色幽默片"——方差很小，但 CTR 差异很大。
>
> DNN 第一层是**有监督 projection**，梯度会告诉模型哪些维度对预测有用，所以比 PCA 好。

---

## 六、细节处理和风险点（1 分钟）

> **1. 时间泄漏风险。** 我们在第 1 步用全量数据计算了每部电影的交互次数来做冷启动分层。严格来说，测试集的数据不应该参与这个统计。但这里冷启动分层只是为了评估时切片分析，不参与训练，所以影响不大。生产环境中冷启动标签应该基于训练集统计。
>
> **2. LLM API 可靠性。** 代码里对 API 调用失败做了 try-except + sleep 重试，但不够健壮。生产环境需要指数退避 + 死信队列 + 降级方案（API 挂了就退化成 Baseline 模型）。
>
> **3. 冷启动定义的争议。** Very Cold / Cold / Warm 的阈值是拍脑袋的。不同业务场景阈值不一样——短视频可能 5 次就算老用户，招聘平台可能 1 次交互都没有。面试可以说"实际项目中我们会根据数据分布动态调阈值，或者用分位数来定义"。
>
> **4. 实验结论的外部有效性。** MovieLens-1M 只有 3706 部电影、6040 个用户，是一个很干净的小数据集。在工业级数据上（千万用户、百万物品）这个结论是否成立？LLM 向量的提升会不会被大规模 ID embedding 稀释？这是需要进一步验证的。

---

## 七、结束（30 秒）

> 总结一下：
> - **功能上**：我们跑通了 5 步 Pipeline，验证了 LLM 语义向量对冷启动 CTR 有效
> - **系统上**：设计了 4 个模块，通过消融实验证明了"完整语义 > PCA 压缩"
> - **工程上**：识别了 3 个待完善项，明确了上线方案的路径
>
> 谢谢大家，欢迎提问。
